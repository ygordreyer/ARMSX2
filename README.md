# ARMSX2 Android PR evidence

Original screenshots and videos from the same device before and after each local patch. These assets are hosted on this separate branch so the code pull requests remain source-only.

- `485/`: Retroid Pocket 5, release 2.7 versus local Android build. The three clamping descriptions retain/remove the obsolete restart warning.
- `726/`: Galaxy Fold 5, release 2.7 versus local Android build. Selecting the Merge Sprite search result leaves the section collapsed/reveals and focuses the setting.
- `742/`: Retroid Pocket 5, release 2.7 versus local Android build. The recorded input sequence tests Select + R1, then releases Select while R1 remains held. Ygor independently confirmed the behavior using the physical RP5 controls.

Game files, BIOS files, and log data are not included.
